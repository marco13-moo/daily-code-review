{
  "name": "daily-code-review",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A guarded GitHub bot that performs useful daily pull-request analysis.",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --check src/index.js && node --check src/analyzer.js && node --check src/github.js && node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
